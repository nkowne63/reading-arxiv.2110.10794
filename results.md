# Results（日本語訳）

ここでは主結果を示す。すなわち、量子LDPC符号において、低オーバーヘッド性を保ったまま lattice surgery を一般化することで耐故障な論理ゲートを実装する手順である。我々は、多論理（multi-logical）パウリ測定（Litinski19）を用いて量子LDPC符号上の耐故障ゲートを実装する方法を提案する。本結果は、量子符号のスタビライザ生成子の重みを下げるために元々考案された一連の手法（Hastings16; Hastings21）を拡張するものである。さらに我々は、すべての論理パウリ演算子の測定を含むように拡張し、論理Clifford群全体を実装できるようにする。本手法はコード変形の一形態であり、符号を新しい符号へ変形し、その過程で元の符号に関する論理情報を得る（Raussendorf07; Fowler12; Horsman12; Brown17; Litinski19; Vuillot19）。

我々の構成により、単一量子ビットのパウリ測定に加え、1つまたは複数のLDPCブロック上の論理量子ビット間のパリティ測定を、任意のパウリ基底で実行できる。この能力は、完全なCliffordゲート集合を測定ベースで実現するためのアプローチを与える（Litinski19）。このゲート集合は、マジック状態蒸留と状態注入を組み合わせることで普遍量子計算へ拡張できる（Bravyi05）。普遍量子計算については Section `sec:FTQC` で詳述する。また我々の構成は、コード変形の間も符号距離が保たれることを保証し、したがって誤り訂正能力が維持される。

以下では、まず Section `sec:ldpc` で基本的な記法と用語を定める。次に Section `sec:deformation` で、論理多量子ビットパウリ演算子を測定するための構成を示す。その後 Section `sec:fault_tol` で、構成が過程全体を通して符号距離を保存することを証明する。

## 記法と用語（Notation and terminology）

量子誤り訂正符号はスタビライザ形式で記述する。パウリ群を $\mathcal{P}=\langle I,X,Y,Z\rangle$、$n$量子ビットに作用するパウリ群を $\mathcal{P}_n=\mathcal{P}^{\otimes n}$ とする。スタビライザ符号は、$-I\notin\mathcal{S}$ を満たす可換（Abelian）部分群 $\mathcal{S}\subset \mathcal{P}_n$ によって定義される。符号 $\mathcal{C}$ は、$\mathcal{S}$ の各演算子の共通 $+1$ 固有値固有状態によって張られる部分空間である。論理パウリ演算子とは、$\mathcal{S}$ のすべての演算子と可換だが $\mathcal{S}$ 自身には属さない $\mathcal{P}_n$ の演算子である。$\mathcal{S}$ が独立な生成子集合 $\{g_1,\dots,g_m\}$ で生成されるとき、論理量子ビット数は $k=n-m$ である。符号距離は、非自明な論理演算子のうち最小重みのものの重みに等しい。ここで、$\mathcal{P}_n$ の演算子の重みとは、恒等でない作用を受ける量子ビット数（非恒等サポートのサイズ）である。特に重要なクラスとして、Calderbank–Shor–Steane（CSS）符号がある。CSS符号では、スタビライザ群は「Pauli-$X$ のみの積」からなる要素と「Pauli-$Z$ のみの積」からなる要素の2種類だけを含む生成集合で生成できる。

次に、スタビライザ符号族 $\mathcal{S}_n$（各メンバーが量子ビット数 $n$ によって添字付けされる）を考える。$\mathcal{S}_n$ のある生成集合に対して、生成子スタビライザの最大重みを $w_n$、任意の1量子ビットに作用するスタビライザ生成子の最大個数を $q_n$ とする。LDPC符号族とは、$w_n=O(1)$ かつ $q_n=O(1)$ を満たす生成集合で生成できるような列 $\mathcal{S}_n$ のことである。以後、$n$ への明示的依存は省略し、これらが定数であると仮定する。

LDPC符号は Tanner グラフで記述できる。$\mathcal{T}=(V,C,E)$ を二部グラフとする。$V$ の各ノードは符号の物理量子ビットに対応し、$C$ の各ノードはスタビライザ群の生成子に対応する。$c\in C$ に対応する生成子が $v\in V$ に対応する量子ビットに非自明に作用するとき、$c$ と $v$ の間に辺を引く。各辺は、生成子がその量子ビットにどう作用するかに応じて $X$/$Z$/$Y$ のいずれかでラベル付けする。CSS符号の場合、辺ではなく $C$ 側ノードを $X$ 型または $Z$ 型でラベル付けしてもよい。以後、Tanner グラフの変数ノード／チェックノードのラベルを、そのまま対応する物理量子ビット／スタビライザ生成子を指すものとして用いる（記法の乱用）。

## コード変形（Code deformation）

ここでは主たる技術的貢献を示す。まず、コード変形を用いて測定を実装する基本アイデアを概観する。スタビライザ符号 $\mathcal{S}=\langle g_1,\dots,g_m\rangle$ と、測定したい論理演算子 $\~L$ があるとする。我々の手順は、$\~L$ を含む新しい符号の生成集合に $\~L$ を追加し、測定結果を十分信頼できる形で得た後に再び $\~L$ を取り除く操作として解釈できる。しかし、単に $\~L$ を $\mathcal{S}$ に追加しても、一般に $\~L$ は高重みであるため LDPC 符号にはならない。LDPC 符号の本質的性質を保つには、$\~L$ をスタビライザ群に含みつつ、$\~L$ を（理想的には小さく）一定重みの生成子で生成できるスタビライザ符号が必要である。これは、追加量子ビットを含む拡張系を作り、低重みスタビライザ生成子 $S_1,\dots,S_l$ を用意して、それらの積が $\~L$ になるようにすることで達成できる。各スタビライザ生成子の測定結果の積が $\~L$ の測定結果を与える。また、新しい符号のスタビライザが $\mathcal{S}$ の他の論理演算子を生成してしまうと、望まない論理測定が起き計算に影響するため、それを避ける必要がある。

## 図2（原文は省略）

**符号 $\mathcal{C}$ の論理 $\~X$ 演算子の測定。**  
**(a)** $\~X$ のサポート上に制限した $\mathcal{C}$ の Tanner グラフの二部部分グラフ $\mathcal{G}_{\~X}$。黒ノードは $\~X$ のサポートにある量子ビットに対応する変数ノード、赤ノードはそのサポート上の量子ビットに作用する $\mathcal{C}$ の $Z$ 型スタビライザに対応するチェックノード。  
**(b)** (a) の論理 $\~X$ に対応する双対グラフ $\mathcal{G}^T=(V^T_{\~X},C^T_{\~X},E^T_{\~X})$。(a) の $X$ 型生成子と量子ビット、(a) の量子ビットと $Z$ 型生成子の間に1対1対応がある。  
**(c)** 補助系 $\mathcal{G}_{\mathrm{anc}}=\mathcal{G}_{\mathrm{merged}}\setminus\mathcal{G}$ を用いた $\~X$ の測定。$\mathcal{G}_{\mathrm{anc}}$ は (b) の部分グラフ $\mathcal{G}^T_{\~X}$ と (a) の部分グラフ $\mathcal{G}_{\~X}$ を交互の層として重ね、隣接層を結ぶ垂直辺集合 $E_{\mathrm{extra}}$ を加えて構成する。$X$ 生成子の積が論理 $\~X$ となるため、各 $X$ 生成子の測定結果の積が論理 $\~X$ の測定結果になる。符号のマージ後に $\~X$ を測定したら、$\mathcal{C}$ のスタビライザと $\mathcal{C}_{\mathrm{anc}}$ の量子ビットを $Z$ 基底で測定して符号を分離し、元の符号空間へ戻る。

### 論理CSS測定（Logical CSS measurement）

ここでは CSS 符号における論理パウリ演算子の耐故障測定の構成を述べる。具体性のため図2の例で説明する。まず $X$ 論理演算子の測定に集中するが、$X$ と $Z$ を入れ替えれば $Z$ 論理演算子にも同様の手順が成り立つ。$\mathcal{G}=(V,C,E)$ を Tanner グラフで記述される CSS スタビライザ符号 $\mathcal{C}$ とし、特定の $X$ 論理演算子 $\~X$ を測定したいとする。そのために、$\mathcal{G}$ を部分グラフとして含む Tanner グラフで記述される新しい符号 $\mathcal{C}_{\mathrm{merged}}$ へ $\mathcal{C}$ を変形する（$\mathcal{G}_{\mathrm{merged}}\supset \mathcal{G}$）。

目的は、スタビライザ群に $\~X$ を含む新しい LDPC 符号を定義することである。この新しい符号のスタビライザ生成子を測定することで、$\~X$ の測定結果を推定できる。

$\mathcal{G}_{\mathrm{merged}}$ の構成は、$\mathcal{G}$ と、$\~X$ が指定する $\mathcal{G}$ の部分グラフ $\mathcal{G}_{\~X}=(V_{\~X},C_{\~X},E_{\~X})$、および双対グラフ $\mathcal{G}^T_{\~X}=(V^T_{\~X},C^T_{\~X},E^T_{\~X})$ で表す。部分グラフ $\mathcal{G}_{\~X}$ の変数ノード $V_{\~X}$ は $\~X$ を支える量子ビットであり、チェック $C_{\~X}$ は $V_{\~X}$ の量子ビットと Tanner グラフの辺を共有する $Z$ 型スタビライザである。辺 $E_{\~X}\subseteq E$ は、$V_{\~X}$ と $C_{\~X}$ の両方に含まれるノードに接続する辺である（図2(a)）。双対グラフ $\mathcal{G}^T_{\~X}$ は、各 $v\in V_{\~X}$ に対応して $v^T\in C^T_{\~X}$ があり、各 $c\in C_{\~X}$ に対応して $c^T\in V^T_{\~X}$ があるように定める。各辺 $e\in E_{\~X}$（$e=(v,c)$）に対して、対応する辺 $e^T\in E^T_{\~X}$（$e^T=(v^T,c^T)$）を置く（図2(b)）。

以後しばらく、$V'$ が別の $X$ 論理演算子を支えるような真部分集合 $V'\subset V$ が存在しないと仮定し、後で一般の場合との差分を説明する。

$\mathcal{G}_{\mathrm{merged}}$ は、$\mathcal{G}$ に $\mathcal{G}^T_{\~X}$ の $r$ 個のコピーと $\mathcal{G}_{\~X}$ の $r-1$ 個のコピーを重ね合わせ、追加の辺集合 $E_{\mathrm{extra}}\subset E_{\mathrm{merged}}$ で結合して定義する。$\mathcal{G}_{\~X}$ と $\mathcal{G}^T_{\~X}$ のコピーは交互の層として配置する（図2(c)）。$E_{\mathrm{extra}}$ は図2(c) のように隣接する $\mathcal{G}^T_{\~X}$ 層と $\mathcal{G}_{\~X}$ 層を結ぶ。明示化のため、$\mathcal{G}^T_{\~X}[j]$（$1\le j\le r$）と $\mathcal{G}_{\~X}[j]$（$2\le j\le r$）で各コピーを添字付けする。$\mathcal{G}_{\~X}\subseteq\mathcal{G}$ を $\mathcal{G}_{\~X}[1]$ とみなす。以後、$\mathcal{G}^T_{\~X}[j]$ に対応する層を **dual layer**、$\mathcal{G}_{\~X}[j]$ に対応する層を **primal layer** と呼ぶ。また最終層 $\mathcal{G}^T_{\~X}[r]$ を **boundary layer** と呼ぶ。

$\mathcal{G}_{\~X}[j]=(V[j],C[j],E[j])$ および $\mathcal{G}^T_{\~X}[j]=(V^T[j],C^T[j],E^T[j])$ の各要素にも添字を付けて $E_{\mathrm{extra}}$ を定める。$\mathcal{G}_{\~X}$ の各変数／チェックに対し、$\mathcal{G}^T_{\~X}$ には対応するチェック／変数があるので、各 $v[j]\in V[j]$ に対応して $v^T[j]\in C^T[j]$ があり、各 $c[j]\in C[j]$ に対応して $c^T[j]\in V^T[j]$ がある。追加辺 $E_{\mathrm{extra}}$ は、すべての $v,c$ と $1\le j\le r$ について $(v[j],v^T[j])$ と $(c[j],c^T[j])$ を含み、さらにすべての $v,c$ と $2\le j\le r-1$ について $(v[j+1],v^T[j])$ と $(c[j+1],c^T[j])$ を含む。

以上で $E_{\mathrm{extra}}\subset E_{\mathrm{merged}}$ を含め、$\mathcal{G}_{\mathrm{merged}}$ の全要素が定まった。$\mathcal{G}$ は $\mathcal{G}_{\mathrm{merged}}$ の部分グラフである。同様に、$\mathcal{G}^T_{\~X}[j]$ と $\mathcal{G}_{\~X}[j]$ の変数ノードは $\mathcal{G}_{\mathrm{merged}}$ の変数でもある。$\mathcal{G}_{\~X}[j]$ のチェックは $Z$ スタビライザ、$\mathcal{G}^T_{\~X}[j]$ のチェックはマージ後グラフの $X$ スタビライザになる。$E_{\mathrm{extra}}$ により $\mathcal{G}$、$\mathcal{G}_{\~X}[j]$、$\mathcal{G}^T_{\~X}[j]$ が一体化される。資源（パリティ測定に用いる補助系）として $\mathcal{G}_{\mathrm{anc}}=\mathcal{G}_{\mathrm{merged}}\setminus \mathcal{G}$ と呼ぶと便利である。

このようにして $\mathcal{G}_{\mathrm{merged}}$ で定義される新しい符号を構成したので、次の補題で $\~X$ が新しい符号のスタビライザ群の要素であることを示す。

**補題（Lemma `merge`）**  
CSS量子LDPC符号 $\mathcal{C}$ と、その $X$ 論理演算子 $\~X$ を考える。$\~X$ が支える量子ビットの真部分集合上に別の $X$ 論理演算子が存在しないとする。このとき、上の $\mathcal{G}_{\mathrm{merged}}$ 構成により得られる符号 $\mathcal{C}_{\mathrm{merged}}$ はスタビライザ群に $\~X$ を含み、
$$
\prod_{\substack{j=1,\ldots,r\\ v^T[j]\in C^T[j]}} v^T[j]=\~X
$$
が成り立つ。

ここで、$v^T[j]$ はそのチェックノードが表すスタビライザ生成子であるという記法を用いた。

**証明（Proof）**  
$\prod_{j,C^T[j]} v^T[j]$ の $\mathcal{G}_{\mathrm{anc}}$ 上のサポートが自明であり、$V[1]$（すなわち $\~X$ のサポート）上では非自明であることを示す。定義より、スタビライザ $v^T[j]$ は変数 $v[j]\in V[j]$ と $c^T[j]\in V^T[j]$ 上にサポートを持つので、それらに注目する。
まず、各 $Z$ 生成子 $c\in C[1]$ は $\~X$ と可換であるため、$V[1]$ 内の偶数個の量子ビットに接続されねばならない。したがって各物理量子ビット $c^T[k]\in V^T[k]$ は $C^T[k]$ 内の偶数個 $q_X$ の $X$ 生成子に接続される。よって各 $c^T[k]$ は $(X_{c^T[k]})^{q_x}=\mathbf{1}$ を与える。
さらに、$k\ge2$ の各物理量子ビット $v[k]\in V[k]$ はちょうど2つの $X$ 生成子（$v^T[k-1]$ と $v^T[k]$）に接続されるので、$(X_{c^T[j]})^2=\mathbf{1}$ を与える。
最後に、各 $v[1]\in V[1]$ は $\mathcal{G}_{\mathrm{anc}}$ の中でちょうど1つの $X$ 生成子（$v^T[1]$）に接続される。従って $\mathcal{C}_{\mathrm{anc}}$ のすべての $X$ 生成子の積はちょうど論理演算子 $\~X$ となり、主張が従う。□

$\~X$ を実際に測定するには、まず $\mathcal{G}_{\mathrm{merged}}$ を決め、$\mathcal{G}_{\mathrm{anc}}$ の各物理量子ビットを $\lvert 0\rangle$ に準備する。次に $\mathcal{G}_{\mathrm{merged}}$ のすべてのスタビライザ生成子を測定し、誤り訂正を1回行う。測定がノイズを含む場合に耐故障性を確保するため、この手順を $d$ 回繰り返せる（Vuillot19）。耐故障的に $\~X$ の測定結果を得たら、$\mathcal{G}_{\mathrm{anc}}$ の各物理量子ビットを Pauli-$Z$ 基底で測定して元の符号空間 $\mathcal{C}$ に戻る。

補題は $\~X$ の測定を可能にするが、そのサポート内に他の $X$ 論理演算子が存在しない特別な場合に限られる。一般には、$V[1]$ の真部分集合 $V'$ 上に別の $X$ 論理演算子 $\~X'$ が存在しうる。この場合、上の構成に従うと $\~X'$ の望まない測定が起き、計算が変わってしまう。例として、2論理量子ビット演算子 $\~X_1\~X_2$ の測定を考える。ここで $\~X_1$ と $\~X_2$ は互いに物理量子ビットで交差しない標準的（canonical）論理演算子とする。上の手順は $\~X_1$ と $\~X_2$ を別々に測定してしまう。

そこで、図3（`fig:xx`）のように、一般の場合に対応するため $\mathcal{G}_{\mathrm{merged}}$ の構成を一般化する。$\~X_1\~X_2$ を、$\~X_1$ や $\~X_2$ を個別に測らずに測定するには、別々の論理演算子が接続されるように $\mathcal{G}_{\mathrm{merged}}$ を構成する必要がある。まず $\~X_1$ と $\~X_2$ のための補助系 $\mathcal{G}_{\mathrm{anc},1}$ と $\mathcal{G}_{\mathrm{anc},2}$ を別々に作る。$1\le k\le r$ に対して、$c_1[k]\in C^T_1[k]$ と $c_2[k]\in C^T_2[k]$ を任意の $X$ 生成子として選ぶ。次に $1\le k\le r$ の新しい物理量子ビット $a[k]$ と、$1\le j\le r$ の新しい $Z$ 生成子 $z[j]$ を導入する（図3のハイライト領域）。$\mathcal{G}_{\mathrm{merged}}$ のノードは $\mathcal{G}\cup\mathcal{G}_{\mathrm{anc},1}\cup\mathcal{G}_{\mathrm{anc},2}$ の全ノードに加え、$a[k],z[j]$ を含む。辺集合も同様にそれらの全辺に加え、$\mathcal{G}^T_{\~X_1}[2]$ と $\mathcal{G}^T_{\~X_2}[2]$ をそれぞれ $\mathcal{G}_{\~X_1}[1]$ と $\mathcal{G}_{\~X_2}[1]$ に結ぶ辺を含む。さらに、図3のハイライト領域に対応する辺として、$2\le j\le r$ で $(a[j-1],z[j])$ と $(z[j],a[j])$、$1\le k\le r$ で $(c_1[k],a[k])$ と $(a[k],c_2[k])$、$2\le j\le r$ で $(c_1^T[j],z[j])$ と $(z[j],c_2^T[j])$ を加える。すると $\mathcal{G}\setminus\mathcal{G}_{\mathrm{merged}}$ にある全 $X$ チェックの積が $\~X_1\~X_2$ の測定となり、一方で $\mathcal{G}_{\mathrm{anc},1}$ や $\mathcal{G}_{\mathrm{anc},2}$ のみにある $X$ チェックの積は $a[k]$ 上にサポートを持つため $\~X_1$ や $\~X_2$ の個別測定にはならない。

続ける前に、いくつか方向付けのコメントを述べる。第一に、Dennis02 の格子幾何で定義された平面符号の境界上にサポートを持つ論理演算子を測る場合、元の lattice surgery 構成（Horsman12）を回復する。さらに、得られるゲートは、トーラス上に埋め込んだサーフェスコードを資源として定数レート双曲サーフェス符号の論理CSS演算子を測る Breuckmann17 の提案と精神的に近い。我々の方式で必要な追加資源は、定数因子を除けば符号距離に関して同様にスケールすると期待される。ただし我々の技術は論理演算子に特定の構造を要求せず、より広く量子LDPC符号へ適用できる。第二に、ハイパーグラフ積符号（Tillich14）に馴染みのある読者は、$\mathcal{G}_{\mathrm{anc}}$ が $\mathcal{G}_{\~X}$ と、変数数 $r$ の反復符号の Tanner グラフとのハイパーグラフ積になっていることを確認できる。この観察から、$\mathcal{G}_{\mathrm{anc}}$ の変数上に支えられたスタビライザが可換であることは容易に分かる。

## 図3（原文は省略）

**論理演算子 $\~X_1\~X_2$ の測定。**  
まず図2と同様に $\~X_1$ と $\~X_2$ の補助系を作る。次にそれらをハイライト（緑枠）部分のように接続し、その補助系を先と同様に論理 $\~X_1\~X_2$ に接続する。補助系内の $X$ スタビライザ生成子の積は論理 $X_1X_2$ を与える。また左補助系の $X$ スタビライザの積は、ハイライト領域の量子ビットを含むため $X_1$ にはならない（右も同様）。従って、これら生成子の測定結果の積が $X_1X_2$ の測定結果を与える。測定後は元のスタビライザを測って符号空間へ戻れる。

### 論理非CSS測定（Logical non-CSS measurement）

論理パウリ測定により論理Clifford群全体を実装するには、Pauli-$X$ と Pauli-$Z$ の積からなる非CSS論理演算子（Brown17; Litinski19; chamberland2021universal）も測れなければならない。ここでは我々の構成がそのような測定へどう適応できるかを示す。

まず図4に示す論理演算子 $\~Y$ の測定を示す。$\mathcal{C}$ の論理演算子 $\~X$ と、それに対応する $Z$ 論理演算子 $\~Z$ を取ると、対応する $Y$ 論理演算子は $\~Y=i\~X\~Z$ で与えられる。$\~X$ と $\~Z$ は反可換でなければならないので、$\~X$ の任意のサポートと $\~Z$ の任意のサポートは奇数個の量子ビットで交差する必要がある。

$\~Y$ を測定するには、CSS測定 $\~X$ と $\~Z$ を測るための2つの補助系 $\mathcal{G}_{\mathrm{anc}\,\~X}$ と $\mathcal{G}_{\mathrm{anc}\,\~Z}$ を、以下の fusion 手順で組み合わせて補助系を準備する。$A=V_{\~X}[1]\cap V_{\~Z}[1]$ を $\~X$ と $\~Z$ の交差にある量子ビット集合とする。対応するチェック演算子をそれぞれ $C_{\~X}[k]$ と $C_{\~Z}[k]$ と書く。

各 $v\in A$ に対し、$1\le k\le r$ で対応する $X$ 生成子 $v^T_{\~X}[k]\in C^T_{\~X}[k]$ と $Z$ 生成子 $v^T_{\~Z}[k]\in C^T_{\~Z}[k]$ がある。さらに各 $v[1]$ に対し、$2\le j\le r$ で $\mathcal{G}_{\mathrm{anc}\,\~X}$ と $\mathcal{G}_{\mathrm{anc}\,\~Z}$ 内にそれぞれ対応する量子ビット $v_{\~X}[j]$ と $v_{\~Z}[j]$ がある。$\mathcal{G}_{\mathrm{merged}}$ は、$A$ のすべての頂点について $v^T_{\~X}[k]$ と $v^T_{\~Z}[k]$ を除き、代わりに
$$
y[k]=i\,v^T_{\~X}[k]v^T_{\~Z}[k]
$$
で置き換えた上で、$\mathcal{G}_{\mathrm{anc}\,\~X}$ と $\mathcal{G}_{\mathrm{anc}\,\~Z}$ の全ノード・全辺を用いて作る。$v^T_{\~X}[k]$ と $v^T_{\~Z}[k]$ をマージすると余分な自由度が生じるので、$2\le j\le r$ に対してチェックノード $g[j]$ を導入し、$Z$ 辺 $(v_{\~X}[j],g[j])$ と $X$ 辺 $(v_{\~Z}[j],g[j])$ を加えてその自由度を固定する。すると、$\mathcal{G}_{\mathrm{anc}\,\~X}$ の $X$ 生成子の積、$\mathcal{G}_{\mathrm{anc}\,\~Z}$ の $Z$ 生成子の積、そして $y[k]$ の積から $\~Y$ の測定を推定できる。可換性は、$\mathcal{G}_{\mathrm{anc}\,\~X}$ と $\mathcal{G}_{\mathrm{anc}\,\~Z}$ の生成子が（結合前は）異なる量子ビットに作用するため可換であり、唯一問題となる $v^T_{\~X}[1]$ と $v^T_{\~Z}[1]$ については結合して解決したことから確認できる。この手順は、$\~X_1$ と $\~Z_2$ のサポートの交差が空でない場合の $\~X_1\~Z_2$ の測定にも容易に適用できる。

## 図4（原文は省略）

**論理演算子 $\~Y=i\~X\~Z$ の測定。**  
この測定は図3の $\~X_1\~X_2$ 測定に非常に近いが、主要な違いは、論理演算子 $X_1$ と $Z_2$ の補助系を非CSS生成子で接続する点にある。左の $X$ スタビライザ生成子の積、右の $Z$ スタビライザ生成子の積、dual layer にある混合スタビライザ生成子の積を取ると、論理 $\~Y$ が得られる。

さらに、図5に示すように $\~X_1$ と $\~Z_2$ が物理量子ビットで交差しない場合の非CSS積演算子（例：$\~X_1\~Z_2$）も測る必要がある。CSS測定の一般の場合と同様、パリティ測定をするにはまず論理演算子同士を接続しなければならない。$\~X_1$ と $\~Z_2$ の補助系 $\mathcal{G}_{\mathrm{anc},1}$ と $\mathcal{G}_{\mathrm{anc},2}$ を計算する。$1\le k\le r$ で $c_1[k]\in C^T_1[k]$ と $c_2[k]\in C^T_2[k]$ を、それぞれ $X$ 生成子と $Z$ 生成子として取る。$\~X_1\~X_2$ 測定のときと同様、$\mathcal{G}_{\mathrm{anc},1}$ には $c_1[k]$ が定める列から $X$ 生成子の境界が出るように拡張し、$\mathcal{G}_{\mathrm{anc},2}$ には $c_2[k]$ が定める列から $X$ 生成子の境界が出るように拡張する。$\~Y$ 測定と同様に、境界で対応する $X$ 生成子と $Z$ 生成子をマージし、境界上の対応する物理量子ビット間に重み2の $XZ$ 生成子を作る。すると $\mathcal{G}_{\mathrm{anc},1}$ の $X$ 生成子の積、$\mathcal{G}_{\mathrm{anc},2}$ の $Z$ 生成子の積、マージされたスタビライザの積から、$\~X_1$ と $\~Z_2$ を個別に測ることなく $\~X_1\~Z_2$ を測定できる。

## 図5（原文は省略）

**論理演算子 $\~X_1\~Z_2$ の測定。**  
この測定は図3の $\~X_1\~X_2$ 測定に非常に近いが、主要な違いは、論理演算子 $X_1$ と $Z_2$ の補助系を非CSS生成子（緑枠）で接続する点にある。左の $X$ スタビライザ生成子の積、右の $Z$ スタビライザ生成子の積、ハイライト領域の混合スタビライザ生成子の積を取ると、論理 $\~X_1\~Z_2$ が得られる。

### 可換な論理演算子の同時測定（Simultaneous measurement of commuting logical operators）

可換な2つの論理演算子を同時に測りたいとする。サポートが交差しないなら、各測定は独立に同時実行できる。サポートが交差していても、両方が同型（すべて $X$、またはすべて $Z$）なら、我々の構成で依然として独立にそれぞれを測れる。最後に、2つの可換論理演算子が異なる型（例えば $X$ 型測定 $\~X_1$ と $Z$ 型測定 $\~Z_2$）で、かつ交差する場合、交差点数は偶数でなければならない。2つの可換パウリ演算子を $\~L_1,\~L_2$ とし、交差 $V_1[1]\cap V_2[1]$ の中から、$\~L_1$ と $\~L_2$ が $a$ 上で異なるパウリで作用し、同様に $b$ 上でも異なるパウリで作用するような2つの物理量子ビット $a,b$ を取る。このとき、生成子 $a^T[1]\in C^T_1[1]$ と $a'^T[1]\in C^T_2[1]$ は可換でなく、同様に $b^T[1]\in C^T_1[1]$ と $b'^T[1]\in C^T_2[1]$ も可換でない。これを解消するため、$1\le k\le r$ で $a^T[k]$ と $b^T[k]$ を $a^T[k]b^T[k]$ に置き換え、$2\le j\le r$ で $a[j],b[j]$ に作用する重み2の $Z$ スタビライザ生成子を作って、マージで生じた自由度を固定する。同様に $a'^T[k],b'^T[k]$ を $a'^T[k]b'^T[k]$ に置き換え、$a'[j],b'[j]$ に作用する重み2の $X$ スタビライザ生成子を作る。交差は常に偶数個なので、可換でないスタビライザは必ずペアにできる。

## 耐故障性（Fault tolerance）

論理パウリ測定により論理Cliffordゲートを実行する構成を示したので、次にマージ後の符号が誤り訂正能力を保持し、従って耐故障性を保つことを示す。まず我々の構成から直ちに、$\mathcal{C}$ から $\mathcal{C}_{\mathrm{merged}}$ への変形全体を通して符号の LDPC 性が保たれる。

**補題**  
CSS量子LDPC符号 $\mathcal{C}$ 上の論理演算子 $\~L$ を考える。$\mathcal{C}$ におけるスタビライザ生成子の最大重みを $w$、ある物理量子ビットに作用するスタビライザ生成子の最大個数を $q$ とする。$\mathcal{C}$ で $\~L$ を測定して得られる符号 $\mathcal{C}_{\mathrm{merged}}$ に対する同様の量を $w',q'$ とすると、
$$
w',q'\le \max(w+3,\,q+3)
$$
が成り立つ。

この事実は、スタビライザ測定の複雑さを下げ、変形手順中の誤りの拡散を抑える上で重要である。

### $\mathcal{C}_{\mathrm{merged}}$ の距離（Distance of $\mathcal{C}_{\mathrm{merged}}$）

誤り訂正能力を維持するには、コード変形の間も符号が非自明な距離を保つことが重要である。我々は $\mathcal{C}_{\mathrm{merged}}$ の符号距離が $\mathcal{C}$ の距離より小さくならないことを示す。本構成に関する距離の懸念は2つある。第一に、構成によって論理演算子の重みが小さくなってしまう可能性である。補助系上の論理演算子の構造を考えることで、補助系の boundary layer に終端する論理演算子は、境界が十分離れて保たれているため必然的に重みを維持することを示す。第二に、本構成により符号へ新しい論理自由度が加わりうる。我々はそれら追加量子ビットの状態には関心がないので、サブシステム符号の用語（Poulin2005）に合わせてそれらをゲージ量子ビット（gauge qubits）と呼ぶ。ゲージ量子ビットに対応する論理演算子（ゲージ演算子）が、関心対象の論理演算子の距離を下げないことを示す。また、論理量子ビットとゲージ量子ビットに同時に作用する演算子を dressed logical operator と呼ぶ。重みが $d$ より小さい dressed logical operator が存在すると符号距離が低下するため、そのような演算子がないことを確認する必要がある。

以後の議論では `cleaning`（Bravyi_2009）の概念も用いる。論理演算子 $\~L$ が、ある物理量子ビット集合 $A$ から $B$ へ「clean」できるとは、スタビライザ群の要素 $S$ を掛けて等価な論理演算子 $\~L'=S\~L$ を作り、$A$ 上ではサポートが自明で $B$ 上では非自明となるようにできることをいう。

$\mathcal{C}_{\mathrm{merged}}$ の距離は、補助系中の $\mathcal{G}^T_{\~L}$ のコピー数として定義した $r$ に依存する。$\mathcal{C}$ の距離を $d$ とする。図6に示す、2論理量子ビットを符号化するサーフェスコードの論理演算子 $X_1X_2$ の測定を例にすると、$r=1$ とすると符号距離が大きく低下する一方、$r=d$ とすると距離が保たれる。一般に、$r=d$（$d$ は $\mathcal{C}$ の距離）とすれば、変形中の距離が $d$ を下回らないことを保証できることを示す。

## 図6（原文は省略）

**$\mathcal{C}_{\rm merged}$ の距離。**  
**(a)** 2つのサーフェスコードパッチを角で連結して作った符号を考え、上端境界に沿う論理演算子 $X_1X_2$ を測定したいとする。  
**(b)** 測定に使う補助系が1層しかないと、上端と下端の smooth 境界の間に低重みの論理演算子が生じる。  
**(c)** これを緩和するには、$d$ 層の補助系を用いる必要があり、これによりマージ中も符号距離が保たれる。なお、常に $d$ 層が必要とは限らず、符号によっては1層でも十分な場合がある。

$\mathcal{C}$ の論理演算子を測定すると、考慮すべきゲージ自由度が導入されることがある。$\mathcal{C}$ で論理演算子 $\~L$ を測定するとし、一次・双対グラフ $\mathcal{G}_{\~L}[j]$ と $\mathcal{G}^T_{\~L}[j]$ を用いて補助系を構成し、$\mathcal{C}$ とマージして $\mathcal{C}_{\mathrm{merged}}$ を作る。もし $\mathcal{G}^T_{\~L}$ の独立なチェックノード数が変数ノード数以下なら、$\mathcal{G}^T_{\~L}$ は非自明な自由度を持ち、$\~L$ の測定により符号へ追加のゲージ量子ビットが加わる。$\~X$ 測定の場合、補助系の任意の dual layer に完全に含まれる $Z$ ゲージ演算子の標準集合を選べる。例えば $r=1$ とし、$\mathcal{G}^T_{\~L}[1]$ が $n'$ 個の変数ノードと $m'$ 個のチェックノードを含むとする。$\mathcal{G}^T_{\~L}[1]$ を古典符号とみなすと少なくとも $n'-m'$ 個の論理ビットを含む（チェックが線形結合である可能性があるのでこれは下界）。しかし我々の場合、$\mathcal{G}^T_{\~L}[1]$ にはちょうど $n'-m'+1$ 個の論理ビットがある。もしチェックノードのある部分集合の積が恒等になるなら、$\~L$ の中の対応する量子ビットが論理演算子になってしまうが、我々は $\~L$ が論理演算子を支える部分集合を含まないよう要求しているからである。$\mathcal{G}^T_{\~L}[1]$ の論理演算子を cycle operator と呼ぶ。$r=1$ の $\mathcal{G}_{\mathrm{merged}}$ を作ると、$n'$ 量子ビットを追加し $m'$ スタビライザ生成子を追加し、論理量子ビットを1つ減らすので、少なくとも $n'-m'+1$ 個の新しいゲージ量子ビットが加わる。ただし同じ制約が $\mathcal{G}_{\mathrm{merged}}$ にも引き継がれるため、正確に $n'-m'+1$ 個のゲージ量子ビットが加わり、$Z$ ゲージ演算子の標準集合として $\mathcal{G}^T_{\~L}[1]$ の論理演算子（cycle operator）を選べる。これらがすべてのゲージ自由度を定める。

$\mathcal{G}^T_{\~L}$ 上の cycle operator は、$\mathcal{C}$ の元の論理演算子とは独立であり、$\mathcal{C}$ のスタビライザに対応する。$\mathcal{G}^T_{\~L}[1]$ のサイクル上の各量子ビットは、$\mathcal{G}_{\~L}[1]$ の対応するスタビライザ生成子を持つ。これら生成子を掛けることで、cycle operator を `clean` して $\mathcal{C}$ のみにサポートを持つようにでき、その場合 cycle operator は $\mathcal{G}_{\~L}[1]$ にある元のスタビライザの積と等価になる。

最下層 $\mathcal{G}^T_{\~L}[1]$ の cycle ゲージ演算子は、$\mathcal{G}_{\~L}[k]$ のスタビライザ生成子を適用することで他の層 $\mathcal{G}^T_{\~L}[k]$ へ clean できる。従って、異なる層の等価なサイクルは同じゲージ量子ビットに対応する。

図7は $X$ 論理演算子の測定例である。ここでは $\mathcal{G}^T_{\~L}[1]$ の標準的な論理演算子集合を2つのサイクルで構成できるので、ゲージ量子ビットは2つある。この場合サイクルは $Z$ ゲージ演算子に対応する。次の補題は $X$ ゲージ演算子の性質を述べる。

**補題（Lemma `gauge`）**  
$\~X$ 論理演算子を測定した後に得られる符号を $\mathcal{C}_{\mathrm{merged}}$ とする。$\mathcal{C}_{\mathrm{merged}}$ の $Z$ ゲージ演算子を $\~Z_g$ とする。ある論理演算子またはゲージ演算子 $\~O$ が $\~Z_g$ と反可換なら、$\~O$ の重みは少なくとも $r$ である。

**証明**  
$\~O$ が $\~Z_g$ と反可換なら、$\~O$ は $\~Z_g$ のあらゆる表現のサポートと交差しなければならない。$\mathcal{G}_{\mathrm{anc}}$ の各 dual layer には、いずれも $\~Z_g$ を表す等価なサイクルが存在する。これら等価サイクルは互いに素であるため、$X$ ゲージ演算子はすべての dual layer にサポートを持たねばならない。dual layer は $r$ 個あるので、$\~O$ の重みは少なくとも $r$ であり、さらに $\mathcal{G}_{\mathrm{anc}}$ の各 dual layer 上で少なくとも1の重みを持つ。□

従って $\~X$ ゲージ演算子は、上端境界 $\mathcal{G}^T_{\~L}[r]$ から下端の $\mathcal{G}^T_{\~L}[1]$ へと伸び、$\mathcal{C}$ 内で終端する「ストリング」とみなせる。以下では、$\mathcal{C}_{\mathrm{merged}}$ をサブシステム符号として扱ったとき距離が少なくとも $d$ であることを示す。

**定理**  
量子CSS LDPC符号 $\mathcal{C}=\llbracket n,k,d\rrbracket$ と、その論理演算子 $\~L$ を考える。$\~L$ を補助系 $\mathcal{C}_{\mathrm{anc}}$ を用いて $\mathcal{C}$ で測定して得られる符号を $\mathcal{C}_{\mathrm{merged}}$ とする。このとき、$\mathcal{C}_{\mathrm{merged}}$ をサブシステム符号として扱うと、補助系が少なくとも $2d-1$ 層を持つ限り距離は $\ge d$ である。

**証明**  
“Materials and methods” を参照。□

## 図7（原文は省略）

**$\mathcal{C}_{\mathrm{merged}}$ のゲージ演算子。**  
補助系の第1層にある赤・橙のサイクルは $Z$ ゲージ演算子の標準集合を与える。赤サイクルはスタビライザまで同一のゲージ演算子を表す。$Z$ ゲージ演算子は変形により $\mathcal{C}$ のみに乗るようにでき、$\mathcal{G}_{\~X}$ にある元の $Z$ スタビライザの積と等価である。青いストリングは赤サイクルに共役な $X$ ゲージ演算子である。これは共役な $Z$ ゲージ演算子の任意の表現と反可換でなければならないため、グラフ上のすべての赤ゲージ演算子と交差しなければならない。赤ゲージ演算子は補助系の任意の dual layer へ clean できるので、青い $X$（ゲージ）演算子は補助系の各 dual layer 上にサポートを持つ必要がある。補助系として $d$ 個の dual layer を用いるので、$X$ ゲージ演算子の重みは少なくとも $d$ になる。

## 低オーバーヘッド耐故障量子計算（Low-overhead fault-tolerant quantum computation）

ここまでで、論理パウリ演算子を耐故障的に測定するための構成を示した。これらの測定により、論理Cliffordゲートに加え、論理パウリ基底での初期化と測定が可能になる。

ここでは、これが低オーバーヘッド耐故障量子計算にとって何を意味するかを考える。まず、耐故障方式に伴う空間・時間オーバーヘッドを考察し、それらを決める重要な量として **parallelism** が現れることを見る。次に、マジック状態を用いて実装する非Cliffordゲートに伴うオーバーヘッドを議論し、量子LDPC符号で利用可能な復号器について議論する。

### 補助系サイズ（Ancilla system size）

補助系を用いたパウリ測定に伴う空間オーバーヘッドを解析する。距離 $d$ の符号 $\mathcal{C}$ において、重み $w_L$ の論理演算子 $\~L$ を考える。$q$ を、$\mathcal{C}$ においてある物理量子ビットに接続するスタビライザ生成子の最大数とする。このとき $\mathcal{G}_{\~L}$ のチェックノード数は高々 $qw_L/2$ であり、主要項として $\~L$ を測る補助系に必要な物理量子ビット数は
$$
n_a=(1+q/2)\,w_L\,r
$$
で与えられる（式 `eq:size`）。

特に定理1のように $r=d$ とすると、補助系サイズは $w_L d$ に比例する。これは、距離が $O(\sqrt{n})$ でスケールするハイパーグラフ積符号のような符号に対して本方式が特に適していることを意味する。表1で扱った（巡回古典符号から構成される）符号では、標準的な論理演算子集合を選んで、その集合内のすべての論理演算子について $w_L=d$ となるようにできる。

### Parallelism

耐故障方式の空間・時間オーバーヘッドが、論理パリティ測定の重みにどう依存するかを見る。空間と時間にはトレードオフがあり、一般のLDPC符号と比べて特定の符号ではこれらのオーバーヘッドを改善できることを見る。

まず、2つの論理量子ビット間のパリティ測定の手法は、複数論理量子ビット間のパリティ測定へ容易に一般化できる。例えば $\~X_1\~X_2\~Z_3$ を測りたいなら、まず $\~X_1\~X_2$ と $\~Z_3$ を別々に測る補助系を作り、その後それらを接続して $\~X_1\~X_2\~Z_3$ を測る補助系を作ればよい。同様にして任意個の論理量子ビットに対するパウリ測定を実装できる。ただし、この構成で全補助系を同じ量子ビットに接続すると、ある量子ビットが多数のスタビライザ生成子のサポートに含まれてしまい、$\mathcal{C}_{\mathrm{merged}}$ が LDPC 符号ではなくなる可能性があるため注意が必要である。

一方、変形中に符号レートを一定に保ちたい場合、1回のパリティ測定に含められる論理量子ビット数には上限がある。符号が $\Theta(n)$ 個の論理量子ビットを持つとし、全ての $X$ 論理演算子のパリティ測定を行いたいなら、$\Omega(nd)$ 個の補助量子ビットが必要になる。$d>\Omega(1)$ なら追加すべき補助量子ビット数は $\Omega(n)$ を超え、有限レート符号を選んだ利点が計算資源コストで相殺される。従って漸近的観点からは、1回のパリティ測定で測る論理量子ビット数は定数に抑えるべきである。この制約は並列に実行できる論理ゲート数を制限し、結果として時間オーバーヘッドを増加させる。一般に、空間と時間の間にはトレードオフがある。多くの補助系を追加して空間オーバーヘッドを増やせば、非常に大きな multi-logical 測定が可能になり時間オーバーヘッドを下げられる。

これらの概念を、結果概要で定義した耐故障量子計算方式の **parallelism** としてまとめる。parallelism は符号に固有の量ではなく、利用可能な空間・時間オーバーヘッドに応じて選ぶ。例えば量子ビット数（空間オーバーヘッド）が主要制約のプラットフォームでは、時間を犠牲にして parallelism を下げるのが有利である。この空間制約レジームでは、parallelism を2として重み2の論理パウリ測定列を実行し、それをエンタングリングゲートへ容易に変換するのが自然である。

しかし、良い古典LDPC符号から構成されたハイパーグラフ積符号のように、より控えめな資源で同じ parallelism を達成できる追加構造を持つ符号もある。ハイパーグラフ積符号は2次元格子上に配置して、標準的論理演算子のサポートが1つの行または列に完全に含まれるようにできる（Kovalev2013）。各行・列は複数の論理量子ビットを支えられるため、1行または1列全体に対する補助系を構成して複数論理量子ビットを測定できる。漸近的には、各行・列は $\Theta(\sqrt{n})$ 個の物理量子ビットと $O(\sqrt{n})$ 個の論理演算子を含む。従って $d=O(\sqrt{n})$ のハイパーグラフ積符号では、サイズ $O(n)$ の単一補助系で論理重み $O(\sqrt{n})$ のパウリ演算子を測定できる。素朴な方式では $O(wd^2)=O(n^{3/2})$ の補助系が必要だったはずである。具体例として表1の $\llbracket 7938,578,16\rrbracket$ 符号は、2次元格子上で各行・列に $63$ 個の物理量子ビットと同型の論理演算子を $17$ 個含むように配置できる。各量子ビットは高々5つのスタビライザ生成子のサポートに含まれ、距離は16である。式 `eq:size` より、1行（または1列）全体に対する補助系を約 $3528$ 個の物理量子ビットで構成し、最大 $17$ 個の論理演算子を測定できる。またこの配置では、異型の論理演算子は同じ論理量子ビットに対応するときにのみ交差する。従って、各誤り訂正ラウンドで各論理量子ビットが高々1つの論理演算子によってのみ非自明に作用されると仮定すれば、可換論理演算子の同時測定の構成を避けることで、スタビライザ生成子の重みを低く保てる。

ハイパーグラフ積符号上のすべての論理演算子を測れるようにしたい場合は $O(n^{3/2})$ 個の補助量子ビットが必要になる。その結果、方式のレートと距離はそれぞれ $O(n^{2/3})$ と $O(n^{1/3})$ に低下する。なお、2次元の厳密に局所的な符号では必ず $kd^2=O(n)$ の制約を満たす必要があるため（Bravyi2010）、これらの符号パラメータは達成できない。

### マジック状態（Magic states）

量子LDPC符号上でパウリ測定を行う方式が得られたので、これを低オーバーヘッドで普遍な耐故障量子計算を行うより大きな枠組みにどう統合するかを考える。パウリ測定に加えて必要な主要要件は、マジック状態の蒸留である（Bravyi05）。

既に述べた通り、Cliffordゲートだけでは普遍量子計算はできず、$T$ ゲートや CCZ ゲートのような非Cliffordゲートが追加で必要である。非Cliffordゲートの耐故障実装の標準的アプローチはマジック状態の注入である。例えば $T$ ゲートはマジック状態 $\lvert T\rangle$ と Clifford ゲートで実装できる。用いる符号が transversal $T$ ゲートを持たない限り、これらマジック状態を耐故障的に準備するのは一般に難しい。そのため、多数のノイズ付きマジック状態から少数の低誤りマジック状態を作るマジック状態蒸留が耐故障アーキテクチャでしばしば必要となる。

ここでは、全データ量子ビットがハイパーグラフ積符号のような1つのLDPC符号ブロックに格納され、そのデータブロックに対してパウリ測定を行う補助系を併設していると仮定する。マジック状態を蒸留するために別のマジック状態ファクトリを用い、蒸留済みマジック状態を補助系を通じてデータブロックへ注入する。ここではサーフェスコードを用いる既存のマジック状態ファクトリを考える。例として、Gidney21 で必要とされる $10^{10}$ 回の $T$ ゲートを、許容誤り $1\%$ で実行したいとし、各ノイズ付き $\lvert T\rangle$ の誤りが $10^{-3}$ とする。このとき出力誤り率 $10^{-12}$ の蒸留方式が必要になる。そのような蒸留方式は、スタビライザ測定に必要な量子ビットを除けば、約 $15000$ 個の物理量子ビットで実装できる。この蒸留方式を $\llbracket 7938,578,16\rrbracket$ のデータブロックと併用しても、完全サーフェスコード方式と比べ有利なオーバーヘッドを維持できる。ただしこの方式は遅く、1度に1つのマジック状態しか生産できない。Discussion では、LDPC符号を用いてマジック状態蒸留のオーバーヘッドをさらに下げ、低オーバーヘッドを保ったまま生産頻度を上げる可能性を検討する。

### 復号器（Decoders）

量子LDPC符号の復号のための効率的アルゴリズム設計には広範な研究がある。多くは古典LDPC符号の既知アルゴリズムを、量子符号特有の事情に合わせて修正したものである。古典LDPC符号の簡単な復号として bit-flip アルゴリズムがある。Leverrier・Tillich・Zemor（Leverrier15）はこれを量子LDPC符号向けに適応して small-set flip 復号器を設計し、十分な拡張性を持つ量子LDPC符号に対して線形個数の誤りを訂正できることが示された（Fawzi20）。古典LDPC符号の主要復号法は belief propagation（BP）復号である。BP は符号をグラフとして捉え、ノード間で尤度を伝播させる。BP は古典符号では良く動作するが、量子LDPC符号では縮退性のため性能が安定しないことがある。これは、等価な補正の間で選べず、信念が分裂する（split beliefs）状況を生む。そのため量子LDPC符号向けに BP を適応する複数の改良が提案されている。特に Panteleev と Kalachev は BP と ordered statistic decoding（OSD）を組み合わせ、様々な量子LDPC符号で良好に見える復号器を設計した（Panteleev19）。Hastings（Hastings14）は双曲サーフェス符号で定数個の誤りを訂正できる効率的な貪欲復号アルゴリズムを作った。以上より、量子LDPC符号の効率的復号が可能であることを示す研究蓄積は十分にある（Breuckmann16; Breuckmann17; Breuckmann20 も参照）。

復号については、上に挙げたいずれかの復号器を用いると仮定する。良い符号選択ができれば、これら復号器はサーフェスコードのそれと同等、あるいはそれ以上の性能を示すように見える。
