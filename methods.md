# Materials and methods（日本語訳）

## 図8（原文は省略）

**等価な論理演算子。** ここに示す3つの曲線はいずれも等価な $X$ 論理演算子を表す。濃い曲線は元の符号 $\mathcal{C}$ 上の $X$ 論理演算子である。残り2つの論理演算子は、補助系の dual layer にある $X$ スタビライザ生成子を順に適用することで得られる。特に、dual layer で $X$ スタビライザ生成子を適用するたびに、論理演算子は同じ dual layer 上にサポートを持つ。これは、dual layer 内の $X$ スタビライザ生成子の任意の部分集合が独立であるためである。

以下では定理1を言い換えて証明する。

## 定理1（再掲）

量子 CSS LDPC 符号 $\mathcal{C}=\llbracket n,k,d\rrbracket$ と、その論理演算子 $\tilde{L}$ を考える。補助系 $\mathcal{C}_{\textrm{anc}}$ を用いて $\mathcal{C}$ で $\tilde{L}$ を測定した後に得られる符号を $\mathcal{C}_{\text{merged}}$ とする。このとき $\mathcal{C}_{\text{merged}}$ をサブシステム符号として扱うと、補助系が少なくとも $2d-1$ 層を持つ限り距離は $\ge d$ である。

## 証明

簡単のため $\tilde{L}$ が $X$ 論理演算子である場合を証明する。また一般性を失わずに、$\tilde{L}$ が $V_{\tilde{L}}[1]$ の部分集合として $X$ 型スタビライザを含まないと仮定する。より一般的な論理パリティ測定の証明も、ここで展開する方法を用いて最も単純な場合から得られる。

まず $\mathcal{C}$ 内に完全に含まれる $Z$ 論理演算子 $\tilde{Z}$ を考える。$\mathcal{C}$ の $X$ スタビライザは $\mathcal{C}_{\textrm{merged}}$ でも変化しないので、$\tilde{Z}$ は $\mathcal{G}_{\textrm{merged}}\setminus\mathcal{G}_{\textrm{anc}}$ にある物理量子ビット上で依然として少なくとも $d$ の重みを持つ必要がある。次に、ゲージ演算子と任意のスタビライザを適用しても $\tilde{Z}$ の重みが $d$ を下回らないことを示せばよい。$Z$ 論理演算子の重みを下げ得るのは $Z$ 型のスタビライザ／ゲージ演算子のみなので、$Z$ 型のものだけを考えれば十分である。まず $Z$ 生成子 $C[1]\subset \mathcal{G}_{\tilde{L}}[1]$ を適用したときに何が起きるかを見る。これら生成子は、各生成子に $V^T[1]\subset \mathcal{G}^T_{\tilde{L}}[1]$ から1つの追加物理量子ビットが加わった点を除けば、$\mathcal{C}$ の元の生成子と同一である。従って、これら生成子を適用した後も $\tilde{Z}$ は $\mathcal{G}_{\textrm{merged}}\setminus\mathcal{G}_{\textrm{anc}}$ の物理量子ビット上で少なくとも $d$ の重みを保つ。$k\ge2$ のスタビライザ生成子 $C[k]$ は $\mathcal{G}_{\textrm{merged}}\setminus\mathcal{G}_{\textrm{anc}}$ と共通サポートを持たないため、論理演算子の重みを $d$ より小さくすることはできず、むしろ重みを増やす方向にしか働かない。

$X$ 論理演算子を測定したときの $Z$ ゲージ演算子は $\mathcal{G}^T_{\tilde{Z}}[1]$ のサイクルである。先に議論したように、これらサイクルは完全に $\mathcal{C}$ へ clean できる。これらゲージ演算子は $\mathcal{C}$ のスタビライザであり、また $\mathcal{C}$ における $\tilde{Z}$ のサポートは $\mathcal{C}_{\text{merged}}$ でも同じなので、$\mathcal{C}$ のスタビライザを適用しても $\tilde{Z}$ の重みが $d$ を下回ることはない。論理演算子の重みは、任意のスタビライザ生成子を掛けたとしても $d$ によって下から抑えられるためである。

次に、補助系の追加によって任意の $X$ 論理演算子の重みが変わらないことを確認する。$\mathcal{C}$ 内に完全に含まれる $X$ 論理演算子で、$\tilde{X}\ne\tilde{L}$ を取る。任意の $X$ 型スタビライザとゲージ演算子を適用しても $\tilde{X}$ の重みが $d$ を下回らないことを示す必要がある。任意の非自明な $X$ ゲージ演算子を $\tilde{X}_g$ とし、$\tilde{X}_g$ と反可換な任意の $Z$ ゲージ演算子を $\tilde{Z}_g$ とする。このとき $\tilde{X}\tilde{X}_g$ は $\tilde{Z}_g$ と反可換なので、補題 `gauge` より、任意のスタビライザ適用後であっても $\tilde{X}\tilde{X}_g$ の重みは少なくとも $d$ である。

最後に考えるべきケースは、ゲージ演算子を適用せず、任意のスタビライザのみを適用する場合である。これは $\tilde{X}$ が物理量子ビットで $\tilde{L}$ と交差する場合にのみ関係する。この場合、dual layer $\mathcal{G}^T_{\tilde{L}}[1]$ にある $X$ 生成子を適用することで、$\tilde{L}$ と $\tilde{X}$ の交差部分にある量子ビットから $\tilde{L}$ を clean し、補助系の第1 primal layer $\mathcal{G}_{\tilde{L}}[2]$ の等価な量子ビットへ移すことができる。

この操作は、第1 dual layer $\mathcal{G}^T_{\tilde{L}}[1]$ 上にも非自明なサポートを作る（図8参照）。なぜなら、もし $C^T_{\tilde{L}}[1]$ に含まれるある $X$ 生成子の部分集合の積が $V^T_{\tilde{L}}[1]$ 上で自明サポートになると仮定すると、次の矛盾が導かれるからである。すなわち、$C^T_{\tilde{L}}[1]$ の部分集合 $A^T$ が存在して、$V^T_{\tilde{L}}[1]$ の各量子ビットが $A^T$ の生成子と偶数回だけ接続されるとする。すると $V_{\tilde{L}}[1]$ の部分集合 $A$ が存在して、$C_{\tilde{L}}[1]$ の各チェックノードが $A$ の量子ビットと偶数回だけ接続されることになる。このとき $A$ の各物理量子ビットへ $X$ を作用させて演算子 $\tilde{X}_A$ を作ると、$\tilde{X}_A$ は $C_{\tilde{L}}[1]$ の各 $Z$ 生成子と可換になり、従って $\tilde{X}_A$ は $\mathcal{C}$ の $X$ 論理演算子になってしまう。しかし我々は $\tilde{L}$ が部分集合として論理演算子を含まないと仮定したので、そのような $A^T$ は存在しない。なお一般の場合には $V_{\tilde{L}}[1]$ の部分集合が論理演算子を支えることがあり得るが、その場合でも $\mathcal{G}_{\textrm{anc}}$ の第1 dual layer にある等価なスタビライザをすべて適用すれば、2つの補助系を結ぶ量子ビット上にサポートが生じる。

以上より、$V_{\tilde{L}}[1]$ 内の量子ビットから $\tilde{L}$ を clean して $V_{\tilde{L}}[2]$ 内の量子ビットで置き換える一方、$V^T_{\tilde{L}}[1]$ 上に非自明サポートが生じることが分かる。同様にして、$\tilde{L}$ のサポートにある $V_{\tilde{L}}[2]$ の量子ビットから $V_{\tilde{L}}[3]$ の量子ビットへ clean しつつ、$V^T[2]$ 上に非自明サポートを作る、という操作を繰り返せる。$\tilde{X}$ と $\tilde{L}$ の交差にある各量子ビットについて、$\tilde{L}$ を clean していくと、上端境界へ到達するまで常に primal layer $\mathcal{G}_{\tilde{L}}[j]$ に等価な量子ビットが存在する。上端境界に到達すると、少なくとも1つの物理量子ビットが $V_{\tilde{L}}[j]$ 上でサポートされることになり、従って $\tilde{X}$ の重みは常に少なくとも $d$ である。

## 謝辞（Acknowledgements）

量子 LDPC 符号を計算へ用いることについて、初期の議論をしてくださった David Poulin に深く感謝する。また、原稿に関する議論とコメントをくださった Andre Saraiva、Michael Vasmer、Paul Webster に感謝する。

本研究は Australian Research Council の Centre of Excellence in Engineered Quantum Systems (EQUS)（プロジェクト番号 CE170100009）および ARO（助成番号 W911NF-21-1-0007）の支援を受けた。利益相反：著者らは利益相反がないことを宣言する。

著者貢献：LZC、IHK、SDB、BJB を含む全著者が方法論を考案し主結果を導出した。全著者が原稿執筆に貢献した。

データ利用可能性：本研究の結論を評価するために必要なすべてのデータは論文中に含まれている。
