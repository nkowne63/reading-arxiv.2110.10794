# Discussion（日本語訳）

我々は、妥当な規模においても、サーフェスコード方式より有利なオーバーヘッドで耐故障量子計算を実現するために LDPC 符号を用いられることを示した。本方式は lattice surgery を一般化したものであり、マジック状態蒸留と組み合わせることで普遍量子計算を実装できる。

我々の構成は、低オーバーヘッドを維持する上で **parallelism** が主要な制約であることを浮き彫りにする。また、この parallelism の役割は、LDPC 符号を用いた量子計算の他の方式、特に Gottesman による方式（Gottesman14）でも指摘されている。Gottesman14 では、量子計算は符号化された資源状態を準備し、テレポーテーションによって量子ゲートを実行することで進む。任意の資源状態を LDPC 符号ブロックへ準備するために連接（concatenation）方式を用いており、これは無視できないオーバーヘッドを持つ。そのため定数オーバーヘッドを保つには parallelism を固定値に保つ必要がある。これに対し我々の提案では、ハイパーグラフ積符号のような特定の符号族に対して、parallelism を距離とともに増加させられる。さらに、Gottesman14 の方式は必要な漸近的振る舞いを持つ一方で、連接はしばしば誤りしきい値が低く、実用上関心のある領域では望ましくないオーバーヘッドを伴う。コード変形に基づく我々の提案では、量子メモリとして用いた LDPC 符号で得られるしきい値（tremblay2021constantoverhead）と同程度のしきい値が期待できる。

我々の方式におけるオーバーヘッド上の主要なボトルネックは2つあり、以下ではそれらがどのように克服されうるかを述べる。1つ目はマジック状態蒸留である。サーフェスコード方式を用いてもなおオーバーヘッド上の優位性を保てることを議論したが、十分高いマジック状態生成率を得るには一般に複数のマジック状態ファクトリが必要になる。LDPC 符号を用いてマジック状態蒸留方式のオーバーヘッドを改善できる可能性がある。ここではサーフェスコードのアイデアに倣う。Litinski19 では、transversal $T$ ゲートを持つ符号を用いたマジック状態蒸留回路を $Z$ 測定のみを使う形に書き換えることで、低オーバーヘッドを実現する複数の方式が提示されている。これは、$Z$ 型の論理誤りが $X$ 型の論理誤りよりはるかに破壊的でないことを意味し、従って $d_z$ が小さいサーフェスコードで十分になるという発想である。これはハイパーグラフ積符号のような LDPC 符号にも非常に容易に一般化できる。高レートで低距離の古典 LDPC 符号と、高距離の反復符号の積を取ることで、低い $d_z$ と高い $d_x$ を持ち、かつサーフェスコードより高い符号化レートを持つ LDPC 符号を構成できる。これは単一のマジック状態ファクトリ構成のオーバーヘッドを改善するわけではないが、サーフェスコードのファクトリ1台と同じ空間に、同一のファクトリを複数台収められる可能性がある。

このようなマジック状態方式の利点の1つは、用いるハイパーグラフ積符号が1次元方向にのみ非局所である点である。これは量子系によっては望ましい単純化となりうる。一般に、有効な研究方向として、許容する非局所性のレベルを制限しつつ LDPC 符号からどれだけ利得が得られるかを検討することが挙げられる。符号距離と接続性の間には関係があること（Krishna21; delfosse2021bounds）、そして非局所性を制限すると達成可能な距離が制限されること（Baspin21）が示されている。ただし、量子 LDPC 符号の物理実装を実現するには、そのような犠牲が必要となるかもしれない。

2つ目の主要なオーバーヘッド要因は、論理測定に用いる補助系である。我々が示した通り、コード変形中に距離を維持するには、補助系の高さが少なくとも $d$ 必要である。必要な距離を保ちつつこのオーバーヘッドを下げるには、Hastings21 で確立された技術を用いることが考えられる。Hastings21 では、グラフ $\mathcal{G}_{\tilde{L}}$ の拡張性（expansion）を増やすことで、補助系の層数を $d$ に比例させるのではなく定数にできる可能性が提案されている。このアプローチを実装するには、十分な拡張性を持つグラフを決定論的に作る方法が必要となる。高い拡張性を持つ Ramanujan グラフの構成は知られているが、それらを本方式へどう統合するかについては今後の検討が必要である。また、この提案が本論文で考えた規模において有意なオーバーヘッド改善を与えるかは不明であり、むしろより大規模な領域で資源コストの改善を与える可能性がある。さらに、耐故障ゲートの時間オーバーヘッドを調べることも重要である。単発（single-shot）誤り訂正によってコード変形を可能にする補助系（Bombin2015; Fawzi20; delfosse2020short）が見つかれば、測定ベースのゲートを定数時間で実装でき、定数空間**かつ**定数時間オーバーヘッドの耐故障量子計算という魅力的な可能性が開かれる。
